#include "main.h"
/**
 * _islower - checks weather a char is lowercase
 * @c: the char to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
